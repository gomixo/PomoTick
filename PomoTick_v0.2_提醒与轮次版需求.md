# PomoTick v0.2 提醒与轮次版需求

## 版本命名建议

推荐版本名：**v0.2 提醒与轮次版**

命名理由：

- “提醒”对应本版本最重要的响铃震动优化和二次提醒。
- “轮次”对应新增的专注轮次、长休息切换和设置能力。
- 名称简洁，适合作为 MVP 后的第一个功能增强版本。

其他可选命名：

- **v0.2 Watch Flow**
- **v0.2 节奏提醒版**
- **v0.2 专注节奏版**

## 版本目标

优化 PomoTick 在手表上的核心使用体验：计时结束后提醒更可靠，主界面操作更适合小屏幕，番茄钟轮次逻辑更完整，并补充基础设置和统计页面。

本版本仍然保持手表优先，不做手机式复杂功能，不增加过度设计。

## 功能范围

本版本包含：

- 计时结束后的响铃震动提醒状态。
- 30 秒自动停止提醒。
- 3 分钟无操作后的 1 次重复提醒。
- 主 UI 交互改版。
- 专注轮次与长休息逻辑。
- 左滑设置界面。
- 右滑统计界面。
- 方形屏幕和圆形屏幕的自适应布局。

## 1. 响铃震动提醒优化

### 目标

计时结束后，应用必须进入明确的提醒状态，让用户能立刻知道当前阶段已经结束。

### 规则

- 任意计时阶段结束后，应用进入提醒状态。
- 进入提醒状态后，同时发出铃声和震动。
- 进入提醒状态后，展示提醒响应界面。
- 用户可以在提醒界面停止提醒。
- 用户可以通过通知停止提醒。
- 如果用户没有处理，提醒在 30 秒后自动停止。
- 提醒停止后，铃声和震动都必须立即结束。

### 提醒界面

提醒界面需要表达：

- 当前阶段已结束。
- 用户可以停止提醒。
- 用户可以继续下一步操作。

界面不需要复杂信息，重点是大按钮、易点击、状态明确。

## 2. 一次重复提醒

### 目标

避免用户错过第一次提醒后，计时流程长时间停在结束状态。

### 触发规则

计时结束后的第一次提醒结束后，开始观察用户是否有新的动作。

第一次提醒结束包括两种情况：

- 用户手动停止提醒。
- 30 秒后自动停止提醒。

如果在第一次提醒结束后的 3 分钟内，用户没有任何新的动作，则触发 1 次重复提醒。

### “新的动作”定义

以下行为都算新的动作：

- 开启下一阶段计时。
- 重置当前计时。
- 手动推进到下一阶段。
- 开始、暂停、恢复任意计时动作。

### 重复提醒规则

- 只提醒 1 次。
- 响铃和震动同时触发。
- 持续 15 秒。
- 15 秒后自动停止。
- 用户也可以提前停止。
- 重复提醒结束后，不再继续重复提醒。

## 3. 主 UI 改版

### 目标

主 UI 更适合手表小屏操作，减少实体按钮占用空间，让计时环成为主要交互区域。

### 布局要求

- 取消当前主界面的 3 个实体按钮。
- 保留环形计时环。
- 环形区域放大，但不要贴近屏幕边缘。
- 环形计时环与屏幕边缘之间保留适当美观距离。
- 中间显示剩余时间。
- 剩余时间下方显示一个操作 icon。
- icon 根据状态变化：
  - 未开始：显示开始。
  - 运行中：显示暂停。
  - 暂停中：显示继续或开始。

### 点击交互

点击环形中间区域：

- 未开始时，开始计时。
- 运行中时，暂停计时。
- 暂停中时，继续计时。

### 长按交互

长按环形中间区域，弹出 2 个选项：

- 重置时间。
- 切换下一阶段。

弹出选项要适合手表点击，不要太小，不要拥挤。

## 4. 专注轮次与长休息逻辑

### 目标

补全标准番茄钟节奏：多次专注后进入长休息，然后回到普通专注和短休息循环。

### 默认规则

- 默认专注时长：25 分钟。
- 默认短休息时长：5 分钟。
- 默认每经过 3 次专注后，进入 15 分钟长休息。
- 长休息结束后，回到 25 分钟专注和 5 分钟短休息的切换。

### 循环示例

默认 3 次专注轮次：

1. 专注 25 分钟
2. 短休息 5 分钟
3. 专注 25 分钟
4. 短休息 5 分钟
5. 专注 25 分钟
6. 长休息 15 分钟
7. 回到下一轮专注 25 分钟

### 设置规则

- 专注轮次可以在设置中调整。
- 可选范围：2 到 6。
- 修改后，从下一轮完整节奏开始生效。

## 5. 左滑设置界面

### 入口

从主 UI 左滑进入设置界面。

### 设置项

设置界面包含以下内容：

- 专注轮次：
  - 可选范围：2 到 6。
- 长休息时间：
  - 10 分钟。
  - 15 分钟。
  - 20 分钟。
- 响铃开关：
  - 开启响铃。
  - 关闭响铃。

### 设置界面要求

- 适合手表点击。
- 每个设置项清晰可见。
- 避免多级复杂菜单。
- 修改后要有明确的当前选中状态。
- 设置内容不需要解释过多，尽量靠直观控件表达。

## 6. 右滑统计界面

### 入口

从主 UI 右滑进入统计界面。

### 第一屏：今日统计

统计界面默认显示今日数据：

- 今天专注总时间。
- 今天休息总时间。
- 今天专注时间按一天中的不同时段拆开显示。

### 今日分时段统计

专注时间按照一天中的不同时段拆开显示，可采用四段：

- 00:00 - 06:00
- 06:00 - 12:00
- 12:00 - 18:00
- 18:00 - 24:00

显示方式要简单，适合手表浏览。可以用简洁条形、列表或分段信息表达，不要求复杂图表。

### 第二屏：一周统计

在统计界面上滑，显示一周统计：

- 一周专注总时间。
- 一周 7 天的专注时间分布。

一周分布重点是让用户看出哪天专注更多，不需要复杂交互。

## 7. 屏幕适配与视觉要求

### 适配目标

UI 同时考虑：

- 方形屏幕。
- 圆形屏幕。

优先保证 OPPO Watch 4 Pro 方形屏幕体验，但布局方式应尽量自适应，避免在圆形屏幕上出现内容被裁切或按钮贴边。

### 视觉要求

- 整体 UI 简洁。
- 不做过度装饰。
- 不堆叠太多信息。
- 重要操作区域要大。
- 文本不要贴边。
- 按钮和选项要容易点击。
- 主计时界面优先突出时间和当前状态。

### 主界面留白

环形计时区域要尽量大，但需要保留边缘距离：

- 方形屏幕上，环形不贴四边。
- 圆形屏幕上，内容不进入容易被裁切的边缘区域。
- 中间时间和 icon 不互相挤压。

## 验收标准

- 计时结束后，会进入提醒状态，并同时响铃震动。
- 用户可以从提醒界面停止提醒。
- 用户可以从通知停止提醒。
- 未处理时，第一次提醒 30 秒后自动停止。
- 第一次提醒停止后，如果 3 分钟内无新动作，会触发 1 次重复提醒。
- 重复提醒持续 15 秒后自动停止。
- 重复提醒只出现 1 次。
- 主界面没有原来的 3 个实体按钮。
- 点击环形中间区域可以开始、暂停、继续。
- 长按环形中间区域可以选择重置或切换下一阶段。
- 默认每 3 次专注后进入 15 分钟长休息。
- 专注轮次可以设置为 2 到 6。
- 长休息时间可以设置为 10、15、20 分钟。
- 响铃开关可以控制是否响铃。
- 左滑可以进入设置界面。
- 右滑可以进入统计界面。
- 统计界面显示今日专注时间和休息时间。
- 今日专注时间可以按时段拆开显示。
- 统计界面上滑后显示一周专注时间和 7 天分布。
- 方形屏幕和圆形屏幕上，核心内容不贴边、不裁切、不拥挤。
